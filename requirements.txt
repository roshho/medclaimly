# API + frontend
fastapi>=0.116.0
uvicorn>=0.35.0
python-multipart>=0.0.20
streamlit>=1.48.0
requests>=2.33.0
pydantic>=2.11.0

# LLM providers — one OpenAI-compatible client covers DeepSeek (primary) and Grok/x.ai (backup)
openai>=1.101.0

# Voice — ElevenLabs STT/TTS, edge-tts zero-config fallback
elevenlabs>=1.0.0
edge-tts>=7.2.3

# RAG + ML pipeline
scikit-learn>=1.7.1
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=17.0.0
xgboost>=2.1.0
duckdb>=1.0.0
tabulate>=0.9.0
